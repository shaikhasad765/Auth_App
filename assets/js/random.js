console.log("BOAT_AUTH");

// Function to enable pointer events and change cursor style

function getopt() {
    console.log("BOAT_APP"); 

    // Enable pointer events for element
    document.getElementById("otp1").style.pointerEvents = "auto";  

    // Change cursor style to "pointer" for element
    document.getElementById("cursor1").style.cursor = "pointer";  
}
